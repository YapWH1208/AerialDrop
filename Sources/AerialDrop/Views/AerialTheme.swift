import SwiftUI

enum AerialTheme {
    /// A restrained indigo accent; content and system materials provide the rest of the palette.
    static let accent = Color(red: 0.46, green: 0.53, blue: 0.85)
}
